# Pinned below MkDocs 2.0: it drops the plugin system and the theming that Material for MkDocs relies
# on, with no migration path. Material 9.x is the line this site is built and tested with.
mkdocs>=1.6,<2
mkdocs-material==9.7.*
